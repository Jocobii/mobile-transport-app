// Expo ambient types (CSS modules, assets, Metro globals).
// `expo start` also generates a git-ignored `expo-env.d.ts` with this reference; keeping it here lets
// `pnpm typecheck` pass on a fresh clone or in CI without starting Expo first.
/// <reference types="expo/types" />

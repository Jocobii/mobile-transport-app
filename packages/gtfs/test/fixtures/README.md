# Test fixtures

Small, real samples of agency feeds used to test the adapters:

- GTFS-Realtime VehiclePosition and TripUpdate snapshots (MVTA, Metro Transit).
- Trimmed GTFS static files (only the rows needed by the tests).

Keep fixtures small. Full GTFS downloads go in the git-ignored `data/` folder at the repo root.

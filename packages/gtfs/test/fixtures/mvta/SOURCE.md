# Fixture source — mvta

Generated by `packages/gtfs/scripts/make-fixtures.ts` (EPIC-001 §12) from the local, git-ignored
captures in `data/raw/mvta/` — see `data/raw/README.md` for capture times and origin.
Do not hand-edit; re-run the script instead.

## Selection

Route short name `436` (route_id: 436), data only, no
route-specific logic.

## Static (GTFS)

- Source: `data/raw/mvta/gtfs.zip`
- Kept: 128 trips, 3590 stop_times,
  55 stops, 9170 shape points.
- `agency.txt` and `feed_info.txt` are copied whole (not filtered).

## Realtime (GTFS-RT)

- `vehicle-positions.pb` ← `vehicle-positions-2026-09-16T1720.pb`
  (feed header timestamp 1789597197); kept 3
  of 56 entities.
- `trip-updates.pb` ← `trip-updates-2026-09-16T1723.pb`
  (feed header timestamp 1789597408); kept 11
  of 152 entities.
- Entities kept: `trip.route_id` in 436.

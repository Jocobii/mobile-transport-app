# Fixture source — metrotransit

Generated by `packages/gtfs/scripts/make-fixtures.ts` (EPIC-001 §12) from the local, git-ignored
captures in `data/raw/metrotransit/` — see `data/raw/README.md` for capture times and origin.
Do not hand-edit; re-run the script instead.

## Selection

Route short name `54` (route_id: 54), data only, no
route-specific logic.

## Static (GTFS)

- Source: `data/raw/metrotransit/gtfs/`
- Kept: 458 trips, 15739 stop_times,
  95 stops, 6860 shape points.
- `agency.txt` and `feed_info.txt` are copied whole (not filtered).

## Realtime (GTFS-RT)

- `vehicle-positions.pb` ← `vehicle-positions-2026-09-16T1738.pb`
  (feed header timestamp 1789598304); kept 22
  of 812 entities.
- `trip-updates.pb` ← `trip-updates-2026-09-16T1739.pb`
  (feed header timestamp 1789598336); kept 24
  of 975 entities.
- Entities kept: `trip.route_id` in 54.

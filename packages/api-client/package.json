{
  "name": "@transit/api-client",
  "version": "0.0.0",
  "private": true,
  "description": "Typed HTTP client for the transit API, used by the mobile app.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@transit/contracts": "workspace:*"
  },
  "devDependencies": {
    "@transit/config": "workspace:*"
  }
}
